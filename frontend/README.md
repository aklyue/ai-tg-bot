# Как начать

```
npm install - (установка зависимостей)

npm run dev - (запуск локально)

npm run build - (сборка проекта)
```

# Переменные окружения

Для удобста используется лишь одна:

```
VITE_API_URL=http://your-endpoint:PORT
```
